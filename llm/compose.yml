services:
  llm-models-init:
    image: busybox:1.36
    user: root
    command: ["sh", "-c", "chmod 0777 /models"]
    volumes:
      - llm-models:/models
    restart: "no"

  llm:
    image: ghcr.io/mostlygeek/llama-swap:${LLAMA_SWAP_TAG:-cpu}
    container_name: llm
    restart: unless-stopped
    depends_on:
      llm-models-init:
        condition: service_completed_successfully
    environment:
      LLAMA_CACHE: /models
    command: ["-config", "/app/config.yaml", "-listen", ":8080", "-watch-config"]
    volumes:
      - llm-models:/models
      - ./config.yml:/app/config.yaml:ro
    ports:
      - "${BIND_ADDR:-127.0.0.1}:8090:8080"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8080/health"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 1m
    deploy:
      resources:
        limits: { cpus: "16.0", memory: 26g }
    logging:
      driver: json-file
      options: { max-size: "10m", max-file: "3" }

  llm-sd-exporter:
    build:
      context: ./exporter
    container_name: llm-sd-exporter
    restart: unless-stopped
    depends_on:
      llm:
        condition: service_healthy
    environment:
      LLM_HOST: llm:8080
      POLL_INTERVAL: 15
      PYTHONUNBUFFERED: "1"
    volumes:
      - llm-sd:/data
    deploy:
      resources:
        limits: { cpus: "0.25", memory: 64m }
    logging:
      driver: json-file
      options: { max-size: "10m", max-file: "3" }

volumes:
  llm-models:
  llm-sd: